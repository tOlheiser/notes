# Arrays

## Declaring and Creating Arrays
```c#
// Declare an int array, c, specifying the number of elements.
int[] c = new int[12];

// Initialize an Array
int[] n = {10, 20, 30, 40, 50}; // declare an array with a set of values.
```

## Using foreach 
```c#
foreach (number in numbers) {
    total += number;
}
```